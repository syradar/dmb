---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString("sv-se", {
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}
</time>
